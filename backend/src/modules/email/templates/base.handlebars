<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title>{{subject}}</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    
    body {
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      line-height: 1.6;
      /* Light Mode: Cor do texto escuro, Fundo claro */
      color: #333333; /* Texto principal */
      background-color: #F8F9FA; /* Fundo do body */
      -webkit-text-size-adjust: 100%;
      margin: 0;
      padding: 20px 0;
      min-height: 100vh;
    }
    
    table { 
      border-collapse: collapse; 
      mso-table-lspace: 0pt; 
      mso-table-rspace: 0pt; 
    }
    
    img { 
      border: 0; 
      line-height: 100%; 
      outline: none; 
      text-decoration: none; 
      display: block; 
      max-width: 100%;
    }
    
    /* Container Principal */
    .email-wrapper {
      max-width: 640px;
      margin: 0 auto;
      background-color: #FFFFFF; /* Fundo do Wrapper Branco */
      border-radius: 16px;
      overflow: hidden;
      /* Sombra suave */
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05), 0 0 0 1px rgba(63, 81, 181, 0.1);
    }
    
    /* Header (Azul) */
    .email-header {
      background: linear-gradient(135deg, #3F51B5 0%, #303F9F 100%); /* Tons de Azul Índigo */
      padding: 48px 40px;
      text-align: center;
      position: relative;
      overflow: hidden;
    }
    
    .email-header::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      /* Padrão em cor clara/azul suave */
      background: url('data:image/svg+xml,<svg width="100" height="100" xmlns="http://www.w3.org/2000/svg"><defs><pattern id="grid" width="40" height="40" patternUnits="userSpaceOnUse"><path d="M 40 0 L 0 0 0 40" fill="none" stroke="rgba(255,255,255,0.1)" stroke-width="1"/></pattern></defs><rect width="100" height="100" fill="url(%23grid)"/></svg>');
      opacity: 0.8; /* Menor opacidade para Light Mode */
    }
    
    .email-header h1 {
      color: #ffffff;
      font-size: 32px;
      font-weight: 700;
      margin: 0;
      position: relative;
      z-index: 1;
      letter-spacing: -0.5px;
    }
    
    /* Body */
    .email-body {
      padding: 48px 40px;
      background-color: #FFFFFF; /* Fundo do Body Branco */
    }
    
    .email-body h2 {
      color: #3F51B5; /* Título em Azul Índigo */
      font-size: 28px;
      margin-bottom: 24px;
      font-weight: 700;
      letter-spacing: -0.5px;
    }
    
    .email-body p {
      color: #555555; /* Texto um pouco mais claro */
      font-size: 16px;
      margin-bottom: 16px;
      line-height: 1.7;
    }
    
    .email-body li {
      color: #555555;
      font-size: 16px;
      margin-bottom: 12px;
      line-height: 1.7;
    }
    
    /* Button (Azul) */
    .button {
      display: inline-block;
      padding: 16px 40px;
      background: linear-gradient(135deg, #42A5F5 0%, #2196F3 100%); /* Tons de Azul */
      color: #ffffff !important;
      text-decoration: none;
      border-radius: 12px;
      font-weight: 600;
      font-size: 16px;
      margin: 24px 0;
      transition: all 0.3s ease;
      box-shadow: 0 4px 16px rgba(33, 150, 243, 0.3);
      border: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .button:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 24px rgba(33, 150, 243, 0.4);
    }
    
    /* Info Box */
    .info-box {
      background-color: #F3F5FC; /* Fundo muito suave */
      border-left: 4px solid #42A5F5; /* Borda Azul Claro */
      padding: 24px;
      margin: 24px 0;
      border-radius: 8px;
      border: 1px solid rgba(66, 165, 245, 0.2);
    }
    
    .info-box p {
      margin: 0 !important;
      color: #333333; /* Texto escuro */
      line-height: 1.6;
    }
    
    /* Badges */
    .badge {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 8px 16px;
      border-radius: 24px;
      font-size: 13px;
      font-weight: 600;
      text-transform: uppercase;
      margin-bottom: 20px;
      letter-spacing: 0.5px;
      border: 1px solid rgba(0, 0, 0, 0.05); /* Borda muito clara */
    }
    
    .badge-info { 
      background: rgba(33, 150, 243, 0.1); 
      color: #2196F3; /* Azul */
      border-color: rgba(33, 150, 243, 0.3);
    }
    
    .badge-success { 
      background: rgba(76, 175, 80, 0.1); /* Mantive verde para 'success' padrão */
      color: #4CAF50;
      border-color: rgba(76, 175, 80, 0.3);
    }
    
    .badge-warning { 
      background: rgba(255, 152, 0, 0.1); 
      color: #FF9800;
      border-color: rgba(255, 152, 0, 0.3);
    }
    
    .badge-danger { 
      background: rgba(244, 67, 54, 0.1); 
      color: #F44336;
      border-color: rgba(244, 67, 54, 0.3);
    }
    
    .badge-system { 
      background: rgba(156, 39, 176, 0.1); 
      color: #9C27B0;
      border-color: rgba(156, 39, 176, 0.3);
    }
    /* Footer */
    .email-footer {
      background-color: #F1F4F8; /* Fundo suave no footer */
      padding: 40px;
      text-align: center;
      border-top: 1px solid rgba(0, 0, 0, 0.05);
    }
    
    .email-footer p {
      color: #777777;
      font-size: 14px;
      margin: 8px 0;
    }
    
    .email-footer strong {
      color: #3F51B5; /* Destaque em Azul Índigo */
      font-size: 16px;
    }
    
    .social-links {
      margin: 24px 0;
      display: flex;
      justify-content: center;
      gap: 16px;
    }
    
    .social-links a {
      color: #3F51B5; /* Links sociais em azul */
      text-decoration: none;
      font-weight: 500;
      padding: 8px 16px;
      border-radius: 8px;
      transition: all 0.3s ease;
      border: 1px solid rgba(63, 81, 181, 0.2);
    }
    
    .social-links a:hover {
      background: rgba(63, 81, 181, 0.1);
      border-color: rgba(63, 81, 181, 0.4);
    }
    
    .divider {
      height: 1px;
      background: linear-gradient(to right, transparent, rgba(63, 81, 181, 0.3), transparent);
      margin: 32px 0;
    }
    /* Icon Styles */
    .icon {
      display: inline-block;
      width: 20px;
      height: 20px;
      vertical-align: middle;
    }
    /* Responsividade */
    @media only screen and (max-width: 640px) {
      body { padding: 10px 0; }
      .email-wrapper { 
        border-radius: 0; 
        box-shadow: none; 
      }
      .email-header, .email-body, .email-footer { 
        padding: 32px 24px !important; 
      }
      .email-body h2 { font-size: 24px; }
      .button { 
        display: block; 
        text-align: center; 
        width: 100%; 
      }
      .info-box { padding: 20px; }
      .social-links { flex-wrap: wrap; }
    }
  </style>
</head>
<body>
  <div class="email-wrapper" role="article" aria-label="{{subject}}">
    <div class="email-header">
      <h1>{{companyName}}</h1>
    </div>
    <div class="email-body">
      {{{content}}}
    </div>
    <div class="email-footer">
      <p><strong>{{companyName}}</strong></p>
      <p>Rua Exemplo, 123 - São Paulo, SP</p>
      <p>contato@empresa.com | (11) 1234-5678</p>
      <div class="social-links">
        <a href="#">Facebook</a>
        <a href="#">Instagram</a>
        <a href="#">LinkedIn</a>
      </div>
      <div class="divider"></div>
      <p style="margin-top: 20px; font-size: 13px; color: #3F51B5;">
        © 2025 {{companyName}}. Todos os direitos reservados.
      </p>
      <p style="font-size: 12px; color: #777777; margin-top: 8px; opacity: 0.8;">
        Você está recebendo este email porque está cadastrado em nossa plataforma.
      </p>
    </div>
  </div>
</body>
</html>